// Package slices provides generic helpers that complement the standard
// library's slices package.
//
// It adds bounds-safe access ([At], [AtOr], [First], [Last]),
// length-extension ([EnsureIndex]), and chunking ([Chunk]).
package slices
