The dismiss × — muted by default, filling to `--default` on hover.

```jsx
<CloseButton onClick={close} />
```

Sits in `Modal.Header`, `Drawer`, `Toast` and dismissible `Alert`. Always keep the `label` meaningful when the surrounding context isn't obvious.
