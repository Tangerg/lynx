Month grid on an overlay surface. Selected days fill with `--accent`; days between `selected` and `rangeEnd` take the soft accent with square corners.

```jsx
<Calendar selected={12} rangeEnd={18} onSelect={setDay} />
```
