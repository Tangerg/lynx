The assembled picker: area, hue track, alpha track, hex field — on an overlay panel.

```jsx
<Popover trigger={<ColorSwatch color={brand} />}><ColorPicker value={brand} onChange={setBrand} /></Popover>
```
