The option list behind Select, ComboBox and Dropdown — overlay fill, 12px radius, 8px-rounded rows.

```jsx
<ListBox>
  <ListBox.Section label="Recent">
    <ListBox.Item isSelected showCheck>Production</ListBox.Item>
    <ListBox.Item description="us-east-1">Staging</ListBox.Item>
  </ListBox.Section>
</ListBox>
```

Renamed from v2's `Listbox` (capital B).
