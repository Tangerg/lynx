Type-to-filter select for long lists. `Autocomplete` in the HeroUI docs is the same primitive with free text allowed.

```jsx
<ComboBox options={countries} placeholder="Country" onSelect={setCountry} />
```
