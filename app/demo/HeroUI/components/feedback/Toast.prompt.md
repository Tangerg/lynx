Transient confirmation, stacked bottom-right by `Toast.Region`.

```jsx
<Toast.Region>
  <Toast title="Deployment queued" action={<Button size="sm" variant="ghost">View</Button>} onClose={dismiss}>
    Building commit 8f2a1c.
  </Toast>
</Toast.Region>
```
