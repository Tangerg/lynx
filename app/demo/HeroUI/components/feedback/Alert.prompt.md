Inline status banner in a soft semantic fill — 12px radius, glyph + title + description.

```jsx
<Alert color="warning" title="Usage at 92%">Add a payment method to avoid interruption.</Alert>
```

Use `Alert` for state tied to the page; use `Toast` for transient results of an action.
