Placeholder while content loads — `--default` fill under a 2s shimmer sweep.

```jsx
<Skeleton variant="circle" width={40} height={40} />
<Skeleton variant="text" width="70%" />
```

Mirror the real layout's shape and count so nothing shifts on load.
