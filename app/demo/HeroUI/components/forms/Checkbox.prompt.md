Boolean choice. Unselected it's a field-coloured box with a hairline; selected it fills with `--accent`.

```jsx
<Checkbox defaultSelected description="We'll email you a weekly digest.">Product updates</Checkbox>
```
