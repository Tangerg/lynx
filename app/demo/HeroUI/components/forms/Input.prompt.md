The raw input element. Reach for `TextField` when you want label + description + error wired together.

```jsx
<Input placeholder="you@company.com" />
<Input size="sm" isInvalid />
```

Fields carry no border: they read as recessed fills. Hover deepens the fill, focus adds the 2px accent ring.
