Identity chip — image, or initials on `--default` when there isn't one.

```jsx
<Avatar name="Ada Lovelace" />
<Avatar.Group><Avatar size="sm" name="A B" /><Avatar size="sm" name="C D" /></Avatar.Group>
```

Grouped avatars overlap 8px with a 2px `--background` ring.
