Shortcut keycap — surface fill, hairline ring, muted 11px label.

```jsx
<Kbd keys={['cmd', 'k']} />
<Kbd>Esc</Kbd>
```

Modifiers render as unicode glyphs (⌘ ⇧ ⌥ ⌃ ↵ ⇥). Used in the docs search trigger and in Tooltips.
