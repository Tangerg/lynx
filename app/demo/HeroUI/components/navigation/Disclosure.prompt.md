One expandable section with a leading chevron. Use `Accordion` when there are several that behave as a set; `DisclosureGroup` is the grouped variant in the HeroUI docs.

```jsx
<Disclosure title="Advanced options">…</Disclosure>
```
