// Package builtin implements the Runtime-owned model tools assembled by
// toolset. Files are organized by capability family, while one package keeps
// their construction vocabulary, optional consumer ports, and contract tests
// cohesive instead of creating a directory for every individual tool.
package builtin
