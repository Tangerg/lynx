// Package dispatch adapts JSON-RPC envelopes and wire frames to the shared
// binding-neutral operation endpoint. It owns transport error codes and wire
// shape metadata, but no use-case invocation policy.
package dispatch
