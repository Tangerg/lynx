// Kernel chat panel + stream chrome — the main-pane orchestrator and everything
// around the message list (scroll affordances / error surfaces / split views).

export { ChatPanel } from "./ChatPanel";
