// Composer — the message input: the textarea + attachments (Composer), the
// footer affordances (ComposerFooter), slash-command suggestions, and the
// submit helper. Consumed by the panel's ChatStream and the composer plugin.

export { Composer } from "./Composer";
export { ComposerFooter } from "./ComposerFooter";
export { SlashSuggestions } from "./SlashSuggestions";
