// Chat panel + stream chrome — the panel orchestrator and everything around
// the message list (header / tab bar / scroll affordances / error surfaces).
// Only ChatPanel is consumed outside chat/ (by the kernel chat slot); the rest
// is internal wiring of the panel.

export { ChatPanel } from "./ChatPanel";
